trigger:
  paths:
    include:
    - 'src/ui/*'
  
pool:
  vmImage: 'ubuntu-latest'

steps:
  - task: NodeTool@0
    inputs:    
      versionSpec: '14.x'
      checkLatest: true
        
  - script: |
      npm install
      npm run build
    displayName: 'npm install and build'